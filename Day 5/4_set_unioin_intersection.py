s1 = {1, 45, 6}
s2 = {7, 8, 1, 78}

print(s1.union(s2)) # union of two sets, it returns a new set with all the elements from bothsets, duplicates are automatically removed
print(s1.intersection(s2)) # intersection of two sets, it returns a new set with only the common elements from both sets